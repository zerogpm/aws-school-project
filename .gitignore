.terraform/
*.tfstate
*.tfstate.*
*.tfvars

# Private build log - names account IDs, profiles, and unrelated workloads
MISTAKES.md

# Local front-end config: account-specific, and stale after a destroy
.env*.local

# Detached server output from ./app.sh --start --web
.local-api.log
.local-web.log
